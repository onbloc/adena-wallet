import styled from 'styled-components';
import { View } from '@components/atoms';

export const StyledContainer = styled(View)`
  gap: 8px;
  align-items: flex-start;
  border-radius: 12px;
  border: 1px solid rgba(251, 194, 36, 0.08);
  background: rgba(251, 194, 36, 0.04);
`;
