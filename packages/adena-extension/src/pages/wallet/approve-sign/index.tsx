import ApproveSignContainer from '@containers/approve-sign-container/approve-sign-container';
import React from 'react';

export const ApproveSign = (): JSX.Element => {
  return <ApproveSignContainer />;
};
